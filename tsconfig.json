{
  "//": "Base only. Each workspace has its own tsconfig; run typecheck from a workspace, not here.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "skipLibCheck": true,
    "strict": true
  }
}
